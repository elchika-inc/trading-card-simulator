{
  "name": "@repo/images",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bunx wrangler dev",
    "dev:container": "docker compose up --build",
    "dev:container:bg": "docker compose up --build -d",
    "container:build": "docker build -t image-transformer ./container",
    "container:stop": "docker compose down",
    "deploy": "bunx wrangler deploy",
    "types": "bunx wrangler types"
  },
  "dependencies": {
    "@repo/types": "workspace:*",
    "hono": "^4.10.7"
  }
}
