-- ============================================================================
-- Migration: is_active column for assets
-- ============================================================================
-- アセットのアクティブ状態を管理するカラムを追加
-- card-back タイプで「現在使用中のカード裏面」を指定するために使用
-- ============================================================================

-- is_active カラムが存在しない場合のみ追加
-- SQLiteではALTER TABLE ADD COLUMN IF NOT EXISTSがないため、
-- このマイグレーションは既にカラムがある場合はスキップする必要があります
-- 既に0001_schema.sqlにis_activeが含まれている場合、このマイグレーションは不要です

-- is_active のインデックスを作成（IF NOT EXISTS で冪等に）
CREATE INDEX IF NOT EXISTS idx_assets_is_active ON assets(type, is_active);
